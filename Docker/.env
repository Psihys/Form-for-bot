# Certificate generation
CERT_GEN_COMMAND=openssl req -x509 -newkey rsa:4096 -keyout /certs/key.pem -out /certs/cert.pem -days 365 -nodes -subj "/CN=localhost"

# Frontend build environment
VITE_ARTICLES_URL=http://localhost:5000/api/articles
VITE_USERS_URL=http://localhost:8080/api/users

# Database configuration
DB_NAME=testdb
DB_USER=testuser
DB_PASS=testpass
MYSQL_ROOT_PASSWORD=rootpass