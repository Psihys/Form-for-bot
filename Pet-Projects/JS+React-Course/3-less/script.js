function showFirstMessage(text) {
  console.log(text)
}

showFirstMessage('Hello world!')

// return 

function calc(a, b) {
  return a + b // return позволяет вернуть значение из функции and stop its execution
}
console.log(calc(4, 3))

// arrow function

const calc = (a, b) => a + b;

